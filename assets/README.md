# Assets

每套表情使用独立目录：

```text
assets/<pack-id>/<item-id>.webp
```

推荐规则：

- 目录名和文件名只使用小写英文字母、数字及连字符。
- 静态图片优先转换为 WebP，动画根据兼容性选用 GIF、动画 WebP 或 APNG。
- 发布过的文件不要覆盖；内容变化时创建新文件名。
- 加入文件前确认允许转载和公开分发，并更新根目录的 `ATTRIBUTIONS.md`。

